%header.banner{:role => "banner"}

    
  .outer_header_container
    .calendar_container
      .date_sheet
        .weekday #{l(Time.zone.now, :format => :abbr_day_of_week)}
        .day_of_month #{l(Time.zone.now, :format => :day_of_month)}
        .month #{l(Time.zone.now, :format => :abbr_month_of_year)}
        .names #{AlmanacDay.names(Time.zone.now)}      
    .inner_header_container
      .logotype_header_container
        = link_to_unless(( controller.action_name == "edit" || controller.action_name == "create" || controller.action_name == "update" || controller.action_name == "new"), image_tag( "evenemangskalendern-allom-evenemang.png", :alt => "Evenemangskalendern Alloms logotyp", :title => "Till evenemangskalendern Alloms startsida", :width => "100%"), "/")
   
