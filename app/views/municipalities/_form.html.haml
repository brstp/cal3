.content
  %article{ :class => :article, :role => :main}
    = semantic_form_for @municipality do |f|
      = f.error_messages
      = f.inputs
      = f.actions do
        = f.action :submit, :as => :button
        = f.action :cancel, :as => :link, :label => "Avbryt, gå tillbaka"